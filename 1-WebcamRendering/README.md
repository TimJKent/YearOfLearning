## Summary

Utilize Opengl via SOGLR to create an application that renders a webcam output to a window.

## Build

```
git submodule update --recursive --init

cmake -S . -B build
cmake --build build
```

## Run 

``` .\build\Debug\WebcamRendering.exe ```